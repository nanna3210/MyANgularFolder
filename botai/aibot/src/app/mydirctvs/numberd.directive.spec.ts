import { NumberdDirective } from './numberd.directive';

describe('NumberdDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberdDirective();
    expect(directive).toBeTruthy();
  });
});
