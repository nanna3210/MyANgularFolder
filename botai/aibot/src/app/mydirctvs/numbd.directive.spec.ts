import { NumbdDirective } from './numbd.directive';

describe('NumbdDirective', () => {
  it('should create an instance', () => {
    const directive = new NumbdDirective();
    expect(directive).toBeTruthy();
  });
});
