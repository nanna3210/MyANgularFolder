import { HasPerissionDirective } from './has-perission.directive';

describe('HasPerissionDirective', () => {
  it('should create an instance', () => {
    const directive = new HasPerissionDirective();
    expect(directive).toBeTruthy();
  });
});
